name: OpenWRT-X86
on:
  push:
    branches:
      - main
jobs:
  build:
    runs-on: ubuntu-latest
    env:
      TZ: Asia/Shanghai
    steps:
      - name: 安装依赖
        run: |
          sudo apt-get update && sudo apt-get install -y build-essential subversion asciidoc binutils bzip2 gawk gettext git libncurses5-dev libz-dev patch unzip zlib1g-dev lib32gcc1 libc6-dev-i386 libssl-dev xsltproc wget
      - name: 获取源码
        run: |
          git clone --depth=1 https://github.com/openwrt/openwrt.git openwrt
          cd openwrt
          git pull
          ./scripts/feeds update -a
          ./scripts/feeds install -a
      - name: 配置
        run: |
          cd openwrt
          # 设置设备
          make menuconfig <<EOF
          CONFIG_TARGET_x86=y
          # 增加参数
          CONFIG_TARGET_KERNEL_PARTSIZE=64
          CONFIG_TARGET_ROOTFS_PARTSIZE=512
          # 增加组件
          CONFIG_PACKAGE_autosamba=y
          CONFIG_PACKAGE_ipv6helper=y
          CONFIG_PACKAGE_ip6tables-extra=y
          CONFIG_PACKAGE_ip6tables-mod-nat=y
          # 增加主题
          CONFIG_PACKAGE_luci-theme-argon=y
          CONFIG_PACKAGE_luci-app-argon-config=y         
          # 增加插件
          CONFIG_PACKAGE_luci-app-ttyd=y
          CONFIG_PACKAGE_luci-app-turboacc=y
          CONFIG_PACKAGE_luci-app-openclash=y
          CONFIG_PACKAGE_luci-app-unblockmusic=y
          CONFIG_PACKAGE_luci-app-quickstart=y
          CONFIG_PACKAGE_luci-app-istorex=y
          CONFIG_PACKAGE_luci-app-store=y
          CONFIG_PACKAGE_luci-app-dockerman=y
          CONFIG_PACKAGE_luci-app-wrtbwmon=y
          CONFIG_PACKAGE_luci-app-passwall=y
          # 删除插件
          CONFIG_PACKAGE_luci-app-ipsec=n
          CONFIG_PACKAGE_luci-app-vsftpd=n
          CONFIG_PACKAGE_luci-app-xlnetacc=n
          CONFIG_PACKAGE_luci-app-wol=n
          CONFIG_PACKAGE_luci-app-ddns=n
          CONFIG_PACKAGE_luci-app-vlmcsd=n
          CONFIG_PACKAGE_luci-app-accesscontrol=n
          EOF
      - name: 编译固件
        run: |
          cd openwrt
          make -j$(nproc) download V=s
          make -j$(nproc) || make -j1 V=s
      - name: 上传固件
        uses: actions/upload-artifact@v2  # 使用actions/upload-artifact@v2 Action上传编译好的固件到Github Actions的Artifact中，便于后续下载
        with:
          name: OpenWrt_Firmware  # Artifact的名称，可自定义
          path: ./openwrt/bin/targets  # 上传的目录，需要注意路径是否正确
